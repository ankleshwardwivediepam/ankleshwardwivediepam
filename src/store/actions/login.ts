import {createAction} from '@reduxjs/toolkit';

export const emailLoginRequest = createAction('EMAIL_LOGIN_REQUEST');
export const emailLoginSuccess = createAction('EMAIL_LOGIN_SUCCESS');
export const emailLoginFailure = createAction('EMAIL_LOGIN_SUCCESS');
